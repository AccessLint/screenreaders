#!/bin/sh

set -e

# Turn off splash screen
/usr/libexec/PlistBuddy -c "Set :doNotShowSplashScreen YES" \
  ~/Library/Preferences/com.apple.VoiceOverTraining.plist || \
  /usr/libexec/PlistBuddy -c "Add :doNotShowSplashScreen bool YES" \
  ~/Library/Preferences/com.apple.VoiceOverTraining.plist

# Enable AppleScript
/usr/libexec/PlistBuddy -c "Set :SCREnableAppleScript YES" \
  ~/Library/Preferences/com.apple.VoiceOver4/default.plist || \
  /usr/libexec/PlistBuddy -c "Add :SCREnableAppleScript bool YES" \
  ~/Library/Preferences/com.apple.VoiceOver4/default.plist

sudo /usr/libexec/PlistBuddy -c "Set :SCREnableAppleScript YES" \
  ~/Library/Preferences/com.apple.VoiceOver4/journal.plist || \
  /usr/libexec/PlistBuddy -c "Add :SCREnableAppleScript bool YES" \
  ~/Library/Preferences/com.apple.VoiceOver4/journal.plist


open -a "VoiceOver Utility"

sleep 5

screencapture ~/Desktop/before-enable.png

# start VoiceOver
/System/Library/CoreServices/VoiceOver.app/Contents/MacOS/VoiceOverStarter

open "$1" -a Safari

sleep 5

echo "Copy phrase"
osascript -e 'tell application "VoiceOver"' \
  -e "copy to pasteboard last phrase" \
  -e "grab screenshot vo cursor" \
  -e "end tell"

expected="$2"
actual="$(pbpaste)"

if [[ "$actual" =~ .*"$expected".* ]]; then
  echo "👍 passed assertion"
else
  echo "❌failed 1 assertion. Expected \"$expected\" in \"$actual\""
fi
