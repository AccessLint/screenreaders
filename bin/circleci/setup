#!/bin/sh

set -e

sudo /usr/sbin/sysadminctl -addUser voiceover -fullName "VoiceOver User" -password "$AUTO_VO_PASSWORD" -admin
cp ~/Library/Preferences/com.apple.SetupAssistant.plist /User/voiceover/Library/Preferences
