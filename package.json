{
  "name": "auto-vo",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:ckundo/auto-vo.git",
  "scripts": {
    "build": "source .env && tsc",
    "auth": "yarn build && node auth.js",
    "start": "yarn build && node main.js"
  },
  "type": "module",
  "author": "Cameron Cundiff <cameron@ckundo.com>",
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.12.1",
    "playwright": "^1.12.1",
    "typescript": "^4.3.2"
  }
}
